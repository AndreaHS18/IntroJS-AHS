function add(numer1 = 0, number2 = 0) { // Parémetros
    console.log(numer1 + number2);
}
add(10, 10); // Argumentos o valores reales

const add2 = function(n1, n2) {
    console.log(n1 + n2);
}
add2(3, 3);